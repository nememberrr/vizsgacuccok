Windows Server 2022 PowerShell

Íme a jelentősen kibővített verzió. A **6. és 7. részt** különösen részletes magyarázatokkal láttam el, hogy ne csak a parancsokat lásd, hanem **miért, hogyan és mikor** használd őket.

---

### 6. Active Directory – Részletes Magyarázat

Az **Active Directory PowerShell modul** (ActiveDirectory) lehetővé teszi a domain objektumok (felhasználók, csoportok, számítógépek, OU-k stb.) központi kezelését szkriptekkel. Ez sokkal hatékonyabb, mint a grafikus AD Users and Computers eszköz, különösen nagyobb környezetekben vagy ismétlődő feladatoknál.

#### Modul telepítése és betöltése
```powershell
# Egyszeri telepítés (ha nem Domain Controller)
Install-WindowsFeature RSAT-AD-PowerShell -IncludeManagementTools

# Betöltés (általában automatikus)
Import-Module ActiveDirectory
```
**Magyarázat**: A RSAT modul nélkül nem fogod tudni használni az AD parancsokat nem-DC gépeken. Telepítés után a parancsok azonnal elérhetővé válnak.

#### Általános domain információk
```powershell
Get-ADDomain
Get-ADForest
Get-ADDomainController -Filter * | Select Hostname, Site, OperatingSystem, IPv4Address
```
**Magyarázat**: Ezekkel a parancsokkal gyorsan átlátod a domain struktúrát, mely domain controllerek vannak, milyen site-okban, és milyen verziójúak. Hasznos hibakeresésnél és tervezésnél.

#### Felhasználók kezelése (Users)
```powershell
# Lekérdezés – magyarázat:
# -Identity: konkrét felhasználó
# -Filter: szűrés (pl. csak aktívak)
# -Properties *: minden tulajdonságot kihoz (alapból csak néhányat ad vissza)
Get-ADUser -Identity "pkiss" -Properties *
Get-ADUser -Filter {Enabled -eq $true} -SearchBase "OU=Budapest,DC=contoso,DC=com" |
    Select Name, SamAccountName, Title, Department, LastLogonDate, PasswordLastSet
```

**Új felhasználó létrehozása (részletes)**
```powershell
$jelszo = ConvertTo-SecureString "Jelszo123!" -AsPlainText -Force

New-ADUser -Name "Nagy Anna" `
           -GivenName "Anna" `
           -Surname "Nagy" `
           -SamAccountName "anagy" `
           -UserPrincipalName "anagy@contoso.com" `
           -Path "OU=Users,OU=Budapest,DC=contoso,DC=com" `
           -AccountPassword $jelszo `
           -Enabled $true `
           -ChangePasswordAtLogon $true `
           -Title "Pénzügyi Asszisztens" `
           -Department "Pénzügy" `
           -Company "Contoso Kft." `
           -EmailAddress "anagy@contoso.com" `
           -MobilePhone "+36301234567"
```
**Magyarázat**: A `ConvertTo-SecureString` azért kell, mert a jelszót biztonságosan kell átadni. A `-Path` paraméterrel megadod, mely OU-ba kerüljön. A `-ChangePasswordAtLogon $true` kényszeríti az első bejelentkezéskor a jelszóváltást (biztonsági best practice).

**Tömeges felhasználó létrehozása CSV-ből** (nagyon hasznos!)
```powershell
Import-Csv C:\ujfelhasznalok.csv | ForEach-Object {
    $pass = ConvertTo-SecureString $_.Password -AsPlainText -Force
    New-ADUser -Name $_.FullName `
               -SamAccountName $_.Username `
               -UserPrincipalName "$($_.Username)@contoso.com" `
               -Path $_.OU `
               -AccountPassword $pass `
               -Enabled $true `
               -ChangePasswordAtLogon $true
}
```
**Magyarázat**: CSV importtal több száz felhasználót is létrehozhatsz percek alatt. A CSV fájlban legyenek oszlopok: FullName, Username, Password, OU.

#### Csoportok kezelése (Groups)
```powershell
New-ADGroup -Name "Penzugy-Admins" -GroupScope Global -GroupCategory Security -Path "OU=Groups,DC=contoso,DC=com"

Add-ADGroupMember -Identity "Penzugy-Admins" -Members "anagy","pkiss"
Get-ADGroupMember -Identity "Penzugy-Admins" -Recursive   # beágyazott csoportokkal együtt
```
**Magyarázat**: A `Global` scope általában belső használatra jó, a `Universal` nagyobb erdőkben. A `-Recursive` kapcsolóval látod, ki van a csoportban közvetve is.

#### Számítógépek és OU-k
```powershell
Get-ADComputer -Filter * -Properties * | Select Name, OperatingSystem, LastLogonDate
Move-ADObject -Identity "CN=PC-001,OU=Computers,DC=contoso,DC=com" -TargetPath "OU=Budapest,DC=contoso,DC=com"
```
**Magyarázat**: `Move-ADObject` nagyon hasznos szervezeti átszervezésnél.

---

### 7. Szerver Szerepkörök és Features – Részletes Magyarázat

A Windows Feature-ök és szerepkörök telepítése PowerShell-lel gyorsabb és automatizálható, mint a Server Manageren keresztül.

#### Features kezelése
```powershell
Get-WindowsFeature | Where-Object InstallState -eq "Available" | Sort Name | Format-Table
Install-WindowsFeature Web-Server,Web-Asp-Net45 -IncludeManagementTools -Restart
```
**Magyarázat**: A `-IncludeManagementTools` nélkül csak a szolgáltatás települ, de a kezelőeszközök (konzolok) nem. A `-Restart` automatikusan újraindítja a szervert, ha szükséges.

#### Hyper-V – Részletes
```powershell
# Telepítés
Install-WindowsFeature Hyper-V -IncludeManagementTools -Restart

# VM létrehozása – magyarázat:
# Generation 2 = modernebb (UEFI, Secure Boot)
New-VM -Name "Prod-SRV01" `
       -MemoryStartupBytes 8GB `
       -NewVHDPath "D:\Hyper-V\Prod-SRV01.vhdx" `
       -NewVHDSizeBytes 150GB `
       -Generation 2 `
       -SwitchName "Production"

# Erőforrás optimalizálás
Set-VM -Name "Prod-SRV01" -ProcessorCount 6 -DynamicMemory:$true -MemoryMinimumBytes 4GB -MemoryMaximumBytes 16GB
```
**Magyarázat**: A Dynamic Memory lehetővé teszi, hogy a VM csak annyi memóriát használjon, amennyire valóban szüksége van. Nagyon fontos erőforrás-hatékony környezetekben.

#### IIS (Web Server) – Részletes
```powershell
Install-WindowsFeature Web-Server -IncludeManagementTools

Import-Module WebAdministration

# Weboldal létrehozása
New-IISSite -Name "CegWeb" `
            -BindingInformation "*:80:www.contoso.com" `
            -PhysicalPath "C:\inetpub\wwwroot\CegWeb"

# Application Pool (külön folyamat a stabilitásért)
New-WebAppPool -Name "CegWebPool"
Set-ItemProperty IIS:\AppPools\CegWebPool managedRuntimeVersion v4.0
Set-ItemProperty IIS:\AppPools\CegWebPool enable32BitAppOnWin64 $false

# Alkalmazás (Application) létrehozása
New-WebApplication -Name "api" -Site "CegWeb" -PhysicalPath "C:\inetpub\wwwroot\CegWeb\api" -ApplicationPool "CegWebPool"
```
**Magyarázat**: Az Application Pool különválasztja az alkalmazásokat, így ha egy lefagy, nem viszi magával a többit. Ajánlott minden weboldalnak saját pool-t adni.

---

**Általános PowerShell Tippek a Szerverkezeléshez**

- **WhatIf tesztelés**: `Install-WindowsFeature ... -WhatIf`
- **Hibakezelés**: 
  ```powershell
  Try { New-ADUser ... } Catch { Write-Host "Hiba: $($_.Exception.Message)" -ForegroundColor Red }
  ```
- **Logolás**: `Start-Transcript C:\Logs\$(Get-Date -Format yyyyMMdd_HHmm).txt`

Szeretnéd, hogy még mélyebben kifejtsem valamelyik részt?  
Például:
- Teljes tömeges felhasználó-import szkript magyarázattal
- Hyper-V clustering / live migration
- IIS SSL tanúsítvány automatikus beállítása
- GPO kezelés PowerShell-lel

Mondd meg, melyik részt dolgozzuk ki még részletesebben!

















**Windows Server 2022 PowerShell Cheat Sheet**

Windows Server 2022-ben alapból **PowerShell 5.1** van telepítve (Windows PowerShell). Ajánlott a **PowerShell 7+** telepítése a jobb teljesítmény és kompatibilitás miatt, de sok szerver-szerepkör (AD, Hyper-V, IIS) még a 5.1-es modulokra épül.

### 1. Alapok & Segítség
- `Get-Command` vagy `Get-Command -Module ActiveDirectory` — Összes parancs / modul parancsai
- `Get-Help Get-Service -Examples` / `-Detailed` / `-Full` / `-Online` — Súgó
- `Get-Member` (alias: `gm`) — Objektum tulajdonságai és metódusai (pl. `Get-Process | gm`)
- `Update-Help` — Súgó fájlok frissítése
- `$PSVersionTable` — PowerShell verzió
- `Get-ExecutionPolicy` / `Set-ExecutionPolicy RemoteSigned` — Végrehajtási szabályzat
- `Get-Module -ListAvailable` — Telepített modulok
- `Import-Module ActiveDirectory` — Modul betöltése

**Alias-ek (gyors parancsok)**: `ls`=`Get-ChildItem`, `cd`=`Set-Location`, `pwd`=`Get-Location`, `cat`=`Get-Content`, `rm`=`Remove-Item`, `cp`=`Copy-Item`, `mkdir`=`New-Item`.

### 2. Fájl- és könyvtárkezelés
- `Get-ChildItem . -Recurse -File` (vagy `ls -R`) — Fájlok listázása
- `Get-Content file.txt -Tail 10` — Fájl tartalma (utolsó sorok)
- `Set-Content` / `Add-Content` — Írás / hozzáfűzés fájlba
- `Copy-Item source dest -Recurse` — Másolás
- `Move-Item` / `Remove-Item -Recurse -Force` — Áthelyezés / törlés
- `New-Item -ItemType Directory` / `File` — Új mappa/fájl
- `Get-ACL` / `Set-ACL` — Jogosultságok (NTFS)

### 3. Folyamatok és Szolgáltatások
- `Get-Process` (alias: `ps`) / `Stop-Process -Name chrome`
- `Get-Service` / `Start-Service` / `Stop-Service` / `Restart-Service`
- `Get-Service | Where-Object {$_.Status -eq 'Running'}`
- `Get-Process | Sort-Object CPU -Descending | Select -First 10`

### 4. Hálózat (hagyományos netsh/ipconfig helyett)
- `Get-NetAdapter` — Hálózati adapterek
- `Get-NetIPAddress` / `New-NetIPAddress` / `Remove-NetIPAddress` — IP konfiguráció
- `Get-NetRoute` — Útválasztás
- `Test-Connection google.com` (ping)
- `Test-NetConnection -Port 3389` — Port teszt
- `Resolve-DnsName` — DNS lookup
- `Get-NetTCPConnection` — Aktív kapcsolatok (netstat)
- `Get-DnsClientServerAddress` / `Set-DnsClientServerAddress`

**Tűzfal**:
- `Get-NetFirewallRule` / `Enable-NetFirewallRule` / `New-NetFirewallRule`

### 5. Rendszer és Hardver
- `Get-ComputerInfo` — Rendszerinfó
- `systeminfo` vagy `Get-WmiObject win32_operatingsystem`
- `Restart-Computer -Force` / `Stop-Computer`
- `Get-WinEvent -LogName System -MaxEvents 50` — Eseménynapló
- `Get-EventLog -LogName System | Where {$_.EntryType -eq 'Error'}`
- `Clear-EventLog -LogName System`
- `Get-HotFix` — Telepített frissítések

### 6. Active Directory (modul: ActiveDirectory)
Először: `Install-WindowsFeature RSAT-AD-PowerShell` (ha nem DC).

**Általános**:
- `Get-ADDomain` / `Get-ADForest`
- `Get-ADDomainController -Filter *`

**Felhasználók**:
- `Get-ADUser -Filter * -Properties *`
- `Get-ADUser username -Properties MemberOf, LastLogonDate`
- `New-ADUser -Name "Teszt" -AccountPassword (ConvertTo-SecureString "Pass123" -AsPlainText -Force) -Enabled $true`
- `Set-ADUser` / `Disable-ADUser` / `Enable-ADUser` / `Unlock-ADUser`
- `Search-ADAccount -LockedOut` / `-AccountDisabled`

**Csoportok**:
- `Get-ADGroupMember "GroupName"`
- `Add-ADGroupMember -Identity "Group" -Members user1,user2`
- `Get-ADGroup -Filter *`

**Számítógépek**:
- `Get-ADComputer -Filter *`
- `Remove-ADComputer`

**OU-k és mozgatás**:
- `New-ADOrganizationalUnit`
- `Move-ADObject`

### 7. Szerver Szerepkörök

**Hyper-V** (modul: Hyper-V):
- `Install-WindowsFeature Hyper-V -IncludeManagementTools`
- `Get-VM` / `New-VM` / `Start-VM` / `Stop-VM`
- `Get-VMSwitch` / `New-VMSwitch`

**IIS** (modul: IISAdministration vagy WebAdministration):
- `Install-WindowsFeature Web-Server -IncludeManagementTools`
- `Get-IISSite` / `New-IISSite`
- `Get-WebBinding` / `New-WebBinding`

**Windows Feature-ök**:
- `Get-WindowsFeature` / `Install-WindowsFeature` / `Uninstall-WindowsFeature`

### 8. Távoli Management (Remoting)
- `Enable-PSRemoting -Force`
- `Enter-PSSession -ComputerName szerver`
- `Invoke-Command -ComputerName szerver -ScriptBlock { Get-Service }`
- `New-PSSession` (több szerverre)

### 9. Scripting Alapok
**Változók**:
- `$a = "szöveg"` / `$szam = 5`
- `$tomb = @("1","2")` / `$hash = @{key="value"}`

**Pipeline**:
- `Get-Process | Where-Object {$_.CPU -gt 100} | Select Name, CPU | Sort CPU -Descending`

**Operátorok** (fontosak):
- `-eq`, `-ne`, `-gt`, `-lt`, `-like "*szöveg*"`, `-match`
- `-and`, `-or`
- `?` (ternary PS7+): `$x -gt 10 ? "High" : "Low"`

**Ciklusok**:
- `foreach ($item in $tomb) { }`
- `ForEach-Object { }` (alias `%`)
- `if () {} else {}` / `switch () {}`

**Export/Import**:
- `Export-Csv -Path file.csv -NoTypeInformation`
- `Import-Csv`
- `ConvertTo-Json` / `ConvertFrom-Json`
- `Out-GridView` — Interaktív tábla

### 10. Hasznos Egyéb
- `Get-WindowsUpdateLog` — Update log
- `wevtutil` vagy `Get-WinEvent` — Részletes naplózás
- `Get-CimInstance` (WMI utód) pl. `Get-CimInstance Win32_LogicalDisk`
- `Start-Process` / `Start-Job` / `Start-ThreadJob` (PS7)
- `Measure-Command { }` — Teljesítmény mérés

### Tippek Windows Server 2022-höz
- **Server Core**-on csak PowerShell van (nincs GUI).
- RSAT eszközök távoli managementhez: `Install-WindowsFeature RSAT-*`
- PowerShell 7 telepítés: Winget vagy hivatalos MSI.
- Legjobb gyakorlat: mindig `Get-Help` és `Get-Command` használatával derítsd ki a pontos paramétereket.
- Szkriptek futtatása: `. .\script.ps1` (current scope) vagy `& .\script.ps1`.

Ez a cheat sheet a leggyakoribb szerver-adminisztrációs feladatokra fókuszál. Részletesebb példákért mindig használd a beépített súgót (`Get-Help <cmdlet> -Examples`).

________________________________________________________________________

**6. Active Directory – Részletes PowerShell kezelés (Windows Server 2022)**

A modul neve: **ActiveDirectory**. Domain Controller-en alapból elérhető, más gépeken telepítened kell:

```powershell
# Telepítés (RSAT)
Install-WindowsFeature RSAT-AD-PowerShell -IncludeAllSubFeature -IncludeManagementTools

# Betöltés (általában automatikus, de biztonság kedvéért)
Import-Module ActiveDirectory
```

### Általános domain infók
```powershell
Get-ADDomain                    # Aktuális domain infók
Get-ADForest                    # Forest infók
Get-ADDomainController -Filter * | Select Hostname, OperatingSystem, Site
Get-ADDefaultDomainPasswordPolicy   # Alap jelszóházirend
```

### Felhasználók kezelése
```powershell
# Felhasználó lekérdezése
Get-ADUser -Identity "tamas" -Properties *          # Minden tulajdonság
Get-ADUser -Filter * -SearchBase "OU=Users,DC=contoso,DC=com" | Select Name, SamAccountName, LastLogonDate

# Új felhasználó létrehozása (részletes példa)
$securePass = ConvertTo-SecureString "SuperTitkosJelszo123!" -AsPlainText -Force

New-ADUser -Name "Teszt Elek" `
           -GivenName "Teszt" `
           -Surname "Elek" `
           -SamAccountName "telek" `
           -UserPrincipalName "telek@contoso.com" `
           -Path "OU=Users,DC=contoso,DC=com" `
           -AccountPassword $securePass `
           -Enabled $true `
           -ChangePasswordAtLogon $true `
           -Title "Rendszergazda" `
           -Department "IT" `
           -EmailAddress "telek@contoso.com"

# Tömeges létrehozás CSV-ből
Import-Csv users.csv | ForEach-Object {
    New-ADUser -Name $_.Name -SamAccountName $_.SamAccountName -Enabled $true `
               -AccountPassword (ConvertTo-SecureString $_.Password -AsPlainText -Force)
}

# Jelszó módosítás
Set-ADAccountPassword -Identity "telek" -Reset -NewPassword (ConvertTo-SecureString "UjJelszo456!" -AsPlainText -Force)

# Tiltás / Engedélyezés / Feloldás
Disable-ADAccount -Identity "telek"
Enable-ADAccount -Identity "telek"
Unlock-ADAccount -Identity "telek"
```

### Csoportok kezelése
```powershell
# Csoport lekérdezése és tagok
Get-ADGroup -Filter * | Select Name, GroupScope, GroupCategory
Get-ADGroupMember -Identity "Domain Admins" -Recursive

# Új csoport
New-ADGroup -Name "IT-Admins" -GroupScope Global -GroupCategory Security -Path "OU=Groups,DC=contoso,DC=com"

# Tag hozzáadás / eltávolítás
Add-ADGroupMember -Identity "IT-Admins" -Members "telek", "jdoe"
Remove-ADGroupMember -Identity "IT-Admins" -Members "jdoe" -Confirm:$false
```

### Számítógépek és egyéb objektumok
```powershell
Get-ADComputer -Filter * | Select Name, OperatingSystem, LastLogonDate
New-ADComputer -Name "PC-TEST01" -Path "OU=Computers,DC=contoso,DC=com"

# Inaktív / zárolt fiókok keresése
Search-ADAccount -AccountDisabled
Search-ADAccount -LockedOut
Search-ADAccount -PasswordNeverExpires:$true
```

### OU-k és struktúra
```powershell
New-ADOrganizationalUnit -Name "Budapest" -Path "DC=contoso,DC=com"
Move-ADObject -Identity "CN=telek,OU=Users,DC=contoso,DC=com" -TargetPath "OU=Budapest,DC=contoso,DC=com"
```

**Tippek**:
- Használj mindig `-Properties *` ha sok infóra van szükséged.
- Szűrőkben: `-Filter "Name -like '*admin*'"` vagy `-Filter {Enabled -eq $true}`.
- Nagy környezetben adj meg `-Server dc01.contoso.com` paramétert.

---

**7. Szerver Szerepkörök és Features kezelése**

### Windows Features telepítése / listázása
```powershell
# Összes elérhető szerepkör és feature listázása
Get-WindowsFeature | Out-GridView   # szép táblázatos nézet

# Telepítés példák
Install-WindowsFeature -Name Web-Server -IncludeManagementTools -IncludeAllSubFeature
Install-WindowsFeature Hyper-V -IncludeManagementTools
Install-WindowsFeature AD-Domain-Services -IncludeManagementTools   # Domain Controller szerepkör

# Telepítés utáni újraindítás automatikus
Install-WindowsFeature File-Services -Restart

# Eltávolítás
Uninstall-WindowsFeature Web-Server -Remove
```

### Hyper-V részletesen (modul: Hyper-V)
```powershell
# Telepítés (ha még nincs)
Install-WindowsFeature Hyper-V -IncludeManagementTools -Restart

# Virtuális gépek kezelése
Get-VM
Get-VM | Select Name, State, CPUUsage, MemoryAssigned, Uptime

Start-VM -Name "WinServer01"
Stop-VM -Name "WinServer01" -Force
Restart-VM -Name "WinServer01"

# Új VM létrehozása (részletes)
New-VM -Name "TestVM" `
       -MemoryStartupBytes 4GB `
       -NewVHDPath "D:\VMs\TestVM.vhdx" `
       -NewVHDSizeBytes 80GB `
       -Generation 2 `
       -SwitchName "Default Switch"

Set-VM -Name "TestVM" -ProcessorCount 4 -DynamicMemory:$true -MemoryMaximumBytes 8GB

# Virtuális kapcsolók
Get-VMSwitch
New-VMSwitch -Name "ExternalSwitch" -NetAdapterName "Ethernet" -AllowManagementOS $true
```

**További hasznos Hyper-V parancsok**:
- `Get-VMHost` – Host beállítások
- `Checkpoint-VM -Name VMName` – Pillanatkép
- `Measure-VM` – Erőforrás-használat
- `Get-VHD` / `Resize-VHD`

### IIS (Web Server) részletesen
Két fő modul létezik: **WebAdministration** (régi) és **IISAdministration** (újabb, ajánlott).

```powershell
# Telepítés
Install-WindowsFeature Web-Server -IncludeManagementTools

Import-Module WebAdministration
# vagy
Import-Module IISAdministration

# Weboldalak kezelése
Get-IISSite
New-IISSite -Name "MySite" -BindingInformation "*:80:" -PhysicalPath "C:\inetpub\MySite"

# AppPool létrehozása
New-WebAppPool -Name "MyAppPool"
Set-ItemProperty IIS:\AppPools\MyAppPool -Name managedRuntimeVersion -Value "v4.0"

# Virtuális könyvtár / Application
New-WebApplication -Name "App" -Site "MySite" -PhysicalPath "C:\inetpub\MySite\App" -ApplicationPool "MyAppPool"

# Binding (több domain / port)
New-WebBinding -Name "MySite" -IPAddress "*" -Port 443 -Protocol https -HostHeader "www.example.com"
```

**További IIS példák**:
- `Start-IISSite`, `Stop-IISSite`, `Restart-IISSite`
- `Get-IISAppPool`, `Start-WebAppPool`
- `Get-WebConfiguration` – Részletes konfiguráció

### Egyéb gyakori szerepkörök
- **File Server**: `Install-WindowsFeature FS-FileServer, FS-DFS-Namespace, FS-DFS-Replication`
- **Print Server**: `Install-WindowsFeature Print-Server`
- **DHCP**: `Install-WindowsFeature DHCP -IncludeManagementTools`
- **DNS**: `Install-WindowsFeature DNS -IncludeManagementTools`

**Tippek a szerepkörökhöz**:
- Mindig használd a `-IncludeManagementTools` kapcsolót.
- Nagyobb telepítésekhez készíts szkriptet, és teszteld `-WhatIf` paraméterrel.
- Telepítés után ellenőrizd: `Get-WindowsFeature | Where Installed`







